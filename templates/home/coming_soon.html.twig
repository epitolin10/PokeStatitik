{% extends 'base.html.twig' %}

{% block title %}{{ pageTitle }} — PokeStatitik{% endblock %}

{% block body %}
    {% include 'partials/_header.html.twig' %}

    <main>
        <section class="section container" style="text-align: center; padding: 120px 0;">
            <h1 class="section__title">{{ pageTitle }}</h1>
            <p class="section__subtitle" style="margin-top: 16px;">Cette page arrive bientôt.</p>
            <a href="{{ path('app_home') }}" class="btn btn--primary" style="margin-top: 32px;">← Retour à l'accueil</a>
        </section>
    </main>

    {% include 'partials/_footer.html.twig' %}
{% endblock %}
